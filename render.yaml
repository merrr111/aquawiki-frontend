services:
  - type: web
    name: aquawiki-ai
    env: python
    region: singapore
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn fish_server:app --host 0.0.0.0 --port 10000
    plan: free
